services:
  - type: web
    name: manfix-platform
    runtime: node
    buildCommand: npm ci && npm run render:build
    startCommand: npm run render:start
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
